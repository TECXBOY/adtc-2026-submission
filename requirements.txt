# requirements.txt — install with: pip install -r requirements.txt
# Or use the project venv: python3.11 -m venv .venv && .venv/bin/pip install -r requirements.txt

# Inference runtime
llama-cpp-python>=0.3.0      # CPU-only GGUF inference via llama.cpp

# Retrieval
scikit-learn>=1.3.0           # TF-IDF retriever (offline, zero network calls)
numpy>=1.24,<2                # compatible with torch 2.2

# API server
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pyyaml>=6.0

# (Optional) Dense retrieval — not used in submission, kept for future reference
# sentence-transformers>=3.0,<4
# faiss-cpu>=1.7
